{
  "mcpServers": {
    "demo-progress": {
      "command": "uv",
      "args": [
        "run",
        "--no-project",
        "--quiet",
        "--with",
        "mcp>=2.1,<3",
        "python",
        "${CLAUDE_PLUGIN_ROOT}/progress_server.py"
      ]
    }
  }
}
