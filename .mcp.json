{
  "mcpServers": {
    "curie-gene-roles": {
      "command": "uv",
      "args": [
        "run", "--quiet", "--with", "mcp>=2.1,<3",
        "python", "${CLAUDE_PLUGIN_ROOT}/patterns/01-hello-server/hello_server.py"
      ]
    },
    "curie-toy-cohort": {
      "command": "uv",
      "args": [
        "run", "--quiet", "--with", "mcp>=2.1,<3",
        "python", "${CLAUDE_PLUGIN_ROOT}/patterns/02-tool-contract/cohort_server.py"
      ]
    },
    "curie-model-library": {
      "command": "uv",
      "args": [
        "run", "--quiet", "--with", "mcp>=2.1,<3",
        "python", "${CLAUDE_PLUGIN_ROOT}/patterns/03-resources-and-prompts/model_library_server.py"
      ]
    }
  }
}
