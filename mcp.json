{
  "$schema": "https://agent-plugins.org/schemas/1.1.0/mcp.schema.json",
  "mcpServers": {
    "curie-gene-roles": {
      "type": "stdio",
      "command": "uv",
      "args": [
        "run", "--quiet", "--with", "mcp>=2.1,<3",
        "python", "${PLUGIN_ROOT}/patterns/01-hello-server/hello_server.py"
      ]
    },
    "curie-toy-cohort": {
      "type": "stdio",
      "command": "uv",
      "args": [
        "run", "--quiet", "--with", "mcp>=2.1,<3",
        "python", "${PLUGIN_ROOT}/patterns/02-tool-contract/cohort_server.py"
      ]
    },
    "curie-model-library": {
      "type": "stdio",
      "command": "uv",
      "args": [
        "run", "--quiet", "--with", "mcp>=2.1,<3",
        "python", "${PLUGIN_ROOT}/patterns/03-resources-and-prompts/model_library_server.py"
      ]
    }
  }
}
